# shipinfo-agent-kit

Monorepo for ShipInfo agent-native release artifacts:
- MCP wrapper server
- JavaScript SDK (npm)
- Python SDK (PyPI)
- Registry entry + schemas + examples + publish docs

Default API base URL: `https://shipinfo.net/topos/api`
